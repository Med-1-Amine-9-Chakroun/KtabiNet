{% extends 'adminbase.html.twig' %}

{% block title %}Data{% endblock %}

{% block body %}
    <h1>Data</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ data.id }}</td>
            </tr>
            <tr>
                <th>IdClient</th>
                <td>{{ data.idClient }}</td>
            </tr>
            <tr>
                <th>IdLivre</th>
                <td>{{ data.idLivre }}</td>
            </tr>
            <tr>
                <th>CategorieLivre</th>
                <td>{{ data.categorieLivre }}</td>
            </tr>
            <tr>
                <th>DateConsult</th>
                <td>{{ data.dateConsult ? data.dateConsult|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_data_index') }}">back to list</a>

    <a href="{{ path('app_data_edit', {'id': data.id}) }}">edit</a>

    {{ include('admin/data/_delete_form.html.twig') }}
{% endblock %}
