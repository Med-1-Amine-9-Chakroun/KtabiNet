{% extends "./adminbase.html.twig" %}

{% block title %} Ajouter Livre Reel {% endblock %}

{% block mainContent %}
    <div class="container mt-5">
        <h1>Ajouter Livre Reel</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Ajouter</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

